# Everlore API + worker — copy to .env and fill in values.
# Bun loads .env automatically; see src/config/env.ts for parsing.

# -----------------------------------------------------------------------------
# Required for production (local dev falls back if unset — see env.ts warnings)
# -----------------------------------------------------------------------------

# Symmetric secret for JWT sign/verify (use a long random string in production)
JWT_SECRET=

# MongoDB connection (indexes are created on first connect)
MONGODB_URI=mongodb://localhost:27017/everlore

# Redis — HTTP API, WebSocket pub/sub, BullMQ queues, rate limits, session cache
REDIS_URL=redis://localhost:6379

# OpenAI — embeddings, default chat when not using OpenRouter
OPENAI_API_KEY=

# Pinecone — vector index for lore RAG and memory retrieval
PINECONE_API_KEY=
# Index name in your Pinecone project (vectors upserted per-namespace as lore_{templateId}, mem_{instanceId})
PINECONE_INDEX=nexus-memories

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------

# HTTP port (default 3000)
PORT=3000

# Comma-separated browser origins for CORS (Flutter/web clients)
CLIENT_ORIGINS=http://localhost:3000,http://localhost:8080

# OpenRouter — alternate LLM provider (used by worker/model router when configured)
OPENROUTER_API_KEY=

# Narration models (swap freely for A/B — narration is prose-only, no JSON/tools).
# SFW default is DeepSeek V3.2 via OpenRouter; NSFW default MythoMax. A template
# may override per-world via model_preferences, otherwise these apply.
NARRATION_SFW_MODEL=deepseek/deepseek-v3.2
NARRATION_NSFW_MODEL=gryphe/mythomax-l2-13b

# Google Sign-In verification
# Set this to your Web OAuth client ID so the backend can verify the token audience.
GOOGLE_CLIENT_ID=

# Twilio Verify
# Use AC_MOCK_SID for local development mock mode. In mock mode the accepted OTP is 123456.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=

# Development: set true to skip OTP send/verify rate limits (leave false in production)
DISABLE_OTP_RATE_LIMIT=false

# -----------------------------------------------------------------------------
# Operational notes (not read by this server yet)
# -----------------------------------------------------------------------------

# Google Sign-In requires the Flutter client to request an ID token for GOOGLE_CLIENT_ID.
# Twilio Verify requires phone numbers in E.164 format, e.g. +15551234567.

# ── AWS S3 + CloudFront (media) ──
AWS_REGION=ap-south-1
S3_BUCKET=your-media-bucket
AWS_ACCESS_KEY_ID=your-iam-access-key-id
AWS_SECRET_ACCESS_KEY=your-iam-secret
CDN_BASE_URL=https://your-distribution.cloudfront.net
IMAGE_MODEL=bytedance-seed/seedream-4.5
