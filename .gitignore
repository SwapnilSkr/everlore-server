node_modules
dist
.env
.env.*
!.env.example
*.log
.DS_Store
python_modules
.venv
venv
*.pyc
scripts/tts-output/
# Tier-experiment output (regenerate with corpus:tier). The frozen corpus
# itself IS committed — that is the point of freezing it.
corpus/tier-result.json
